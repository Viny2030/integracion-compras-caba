name: Monitor Diario CABA – Boletín y Compras

on:
  schedule:
    # Corre a las 10:00 UTC (07:00 AM en Argentina)
    - cron: '0 10 * * *'
  workflow_dispatch:   # Permite ejecución manual

permissions:
  contents: write

jobs:
  run-caba:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Descargar código (Checkout)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 📦 Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests beautifulsoup4 openpyxl lxml numpy

      - name: 🏙️ Ejecutar Robot Diario CABA
        run: |
          python diario_caba.py

      - name: 📂 Listar archivos generados (Debug)
        run: |
          ls -R data/ || echo "No se encontró la carpeta data"

      - name: 💾 Commit & Push de Reportes
        run: |
          git config --global user.name 'Robot Monitor CABA'
          git config --global user.email 'robot-caba@noreply.github.com'
          git add data/
          git commit -m "Reporte Diario CABA: $(date +'%Y-%m-%d')" || exit 0
          git push origin HEAD
